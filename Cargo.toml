[package]
name = "readabilities-rs"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "Native readable content extraction, cleanup, and bounded origin acquisition"
repository = "https://github.com/junix/readabilities-rs"
keywords = ["readability", "html", "content-extraction", "scraping"]
categories = ["parser-implementations", "web-programming", "command-line-utilities"]

[[bin]]
name = "readabilities-rs"
path = "src/main.rs"

[[example]]
name = "extraction_benchmark"
path = "examples/extraction_benchmark.rs"
required-features = ["http"]

[features]
default = ["http", "ensemble"]
http = ["dep:futures-util", "dep:reqwest", "dep:tokio"]
ensemble = []

[dependencies]
ammonia = "4.1.4"
clap = { version = "4.6.0", features = ["derive"] }
ego-tree = "0.11.0"
futures-util = { version = "0.3.31", optional = true }
htmd = "0.5.5"
markup5ever_rcdom = "0.38.0"
reqwest = { version = "0.13.4", default-features = false, features = ["brotli", "deflate", "gzip", "rustls", "stream"], optional = true }
scraper = "0.26.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
sha2 = "0.10.9"
thiserror = "2.0.18"
tokio = { version = "1.48.0", features = ["macros", "net", "rt-multi-thread", "time"], optional = true }
unicode-segmentation = "1.12.0"
url = { version = "2.5.8", features = ["serde"] }

[dev-dependencies]
tempfile = "3.23.0"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
too_many_lines = "allow"
struct_excessive_bools = "allow"
